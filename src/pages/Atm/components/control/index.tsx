import React from 'react';
import styles from './control.module.css';

const index: React.FC = () => (
  <div className={styles.control} data-testid="index">
    Control Button
  </div>
);

export default index;
