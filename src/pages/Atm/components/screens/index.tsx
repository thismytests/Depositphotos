import React from 'react';
import styles from './screens.module.css';

const index: React.FC = () => (
  <div className={styles.screens} data-testid="index">
    index Component
  </div>
);

export default index;
